# my-first-pod.yaml
apiVersion: v1
kind: Pod
metadata:
  name: my-pod
  labels:
    app: webserver
spec:
  containers:
  - name: pod-demo
    image: flashaimkevin/gin-app:0.0.1
    ports:
    - containerPort: 8080